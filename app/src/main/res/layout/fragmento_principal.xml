<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:context=".principal.FragmentoPrincipal">

    <androidx.viewpager.widget.ViewPager
            android:id="@+id/viewpager_principal"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:padding="8dp"
            app:layout_constraintTop_toTopOf="parent" app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" android:animateLayoutChanges="false">

        <com.google.android.material.tabs.TabLayout
                android:id="@+id/tabLayout_principal"
                android:layout_width="match_parent"
                android:layout_height="4dp"
                app:tabIndicatorGravity="bottom" app:tabMode="fixed" app:tabIndicatorHeight="4dp"
                app:tabIndicator="@drawable/tab_selector"/>
    </androidx.viewpager.widget.ViewPager>

</androidx.constraintlayout.widget.ConstraintLayout>